# Template only. Keep real values in gitignored `app/.env`.
# The wrapper scripts read this for `--dart-define` injection; never commit populated secrets.
# Allowed client values:
# - SUPABASE_URL
# - SUPABASE_ANON_KEY
# Forbidden here and in repo: SERVICE_ROLE keys, DB passwords, JWT/encryption secrets.
SUPABASE_URL=
SUPABASE_ANON_KEY=
